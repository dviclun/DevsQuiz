import { createTheme } from "@mui/material";
import FredokaMedium from "../assets/fonts/Fredoka-Medium.ttf"

export const theme = createTheme({
    palette: {
        mainBlue: '#00E6F6',
        mainBlack: '#0C0D14',
        secondaryBlack: '#13141E',
        headerColor: '#101116'
    },
});